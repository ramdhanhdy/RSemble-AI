node_modules
dist
dist-ssr
*.local

# Environment files 
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log

# Local coding-agent configuration
WATCHDOG.yml

idea.md
CLAUDE.md
TODOS.md
DECISIONS.md
DESIGN.md
PRODUCT.md
UI.md
docs/archive/idea-original.md
dist-check/
dist-fixed/

# Local browser QA profiles and superseded root captures
.chrome-review*/
ui-review-*.png

# Generated TypeScript build metadata/config output.
# `tsc -b` rewrites these on every typecheck/build, so tracking them would
# dirty the worktree on every gate run. They are regenerated on demand and
# must never be committed.
*.tsbuildinfo
vite.config.js
vite.config.d.ts
vitest.config.js
vitest.config.d.ts
